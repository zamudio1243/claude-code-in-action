Your goal is update any vulnearable dependencies in the project.

Do the following:
1. Run `npm audit` to identify any vulnerable dependencies in the project.
2. Run `npm audit fix` to automatically fix any vulnerabilities that can be resolved.
3. Run test suite to ensure that the updates did not break any functionality.
